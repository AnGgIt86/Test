name: CI
on:
  push:
    branches: [ main ]
jobs:
  container-test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Prepare
        uses: pguyot/arm-runner-action@v2
        with:
          commands: |
            apt-get -y install git nano wget curl bc jq make cmake ninja python3 python-pip golang
            pip install request
            git clone https://github.com/lzhiyong/android-sdk-tools && cd android-sdk-tools
            python get_source.py --tags platform-tools-35.0.2
